@model IEnumerable<AerolineasWEB.Model.Aerolinea>

@{
    ViewData["Title"] = "Inicio";
}
<div class="pt-3 pb-5 d-flex flex-column flex-md-row justify-content-between align-items-md-center">
    <div class="titulos d-flex flex-column ">
        <h1 class="text-primary"><i class="fa-duotone fa-solid fa-building me-1"></i>Aerolíneas</h1>
        <h4 class="text-secondary ms-2">Gestión de la información de Aerolíneas</h4>
    </div>
    <p class="d-flex justify-content-end align-items-center">
        <a class="btn btn-primary d-block w-100" role="button" asp-action="Create"><i class="fa-solid fa-plus me-1"></i>Añadir Aerolínea</a>
    </p>
</div>

<form id="busquedas" class="bg-white border rounded p-3 g-2 mb-5">
    <div class="row">
        <div class="col-md-3 col-sm-12 mt-2">
            <select class="form-select" id="tipo_busqueda" name="tipo_busqueda">
                <option value="nombre" selected="@(ViewBag.BusquedaTipo == "nombre")">Buscar por nombre</option>
                <option value="iata" selected="@(ViewBag.BusquedaTipo == "iata")">Buscar por IATA</option>
                <option value="telefono" selected="@(ViewBag.BusquedaTipo == "telefono")">Buscar por Teléfono</option>
            </select>
        </div>
        <div class="col-md-8 col-sm-12 mt-2">
            <div class="input-icon mb-3">
                <span class="input-icon-addon">
                    <svg xmlns="http://www.w3.org/2000/svg" class="icon" width="24"
                         height="24" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor"
                         fill="none" stroke-linecap="round" stroke-linejoin="round">
                        <path stroke="none" d="M0 0h24v24H0z" fill="none" />
                        <circle cx="10" cy="10" r="7" />
                        <line x1="21" y1="21" x2="15" y2="15" />
                    </svg>
                </span>
                <input type="text" id="valor" name="valor" value="@ViewBag.Valor" class="form-control" placeholder="Buscar Aerolínea" />
            </div>
        </div>
        
        <div class="col-md-1 col-sm-12 mt-2">
            <input type="submit" value="Buscar" class="btn btn-primary d-block w-100" />
        </div>
        @if (ViewBag.Filtro == true)
        {
            <a asp-action="Index" class="text-primary ms-1 fw-normal text-decoration-none">
                <i class="fa-solid fa-filter-circle-xmark"></i> Quitar Filtro
            </a>
        }
    </div>
</form>

@if (TempData["Error"] != null && TempData["FromDelete"] != null)
{
    <div class="alert alert-danger alert-animada" role="alert">
        <div class="alert-icon">
            <!-- Download SVG icon from http://tabler.io/icons/icon/alert-circle -->
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
                 viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                 stroke-linecap="round" stroke-linejoin="round"
                 class="icon alert-icon icon-2">
                <path d="M3 12a9 9 0 1 0 18 0a9 9 0 0 0 -18 0" />
                <path d="M12 8v4" />
                <path d="M12 16h.01" />
            </svg>
        </div>
        <div>
            <h4 class="alert-heading">Hubo un problema al eliminar la aerolínea:</h4>
            <div class="alert-description">
                @TempData["Error"]
            </div>
        </div>
    </div>

}

@if (TempData["Success"] != null && TempData["FromSuccess"] != null)
{
    <div class="alert alert-success alert-animada" role="alert">
        <div class="alert-icon">
            <!-- Download SVG icon from http://tabler.io/icons/icon/check -->
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
                 viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"
                 stroke-linecap="round" stroke-linejoin="round"
                 class="icon alert-icon icon-2">
                <path d="M5 12l5 5l10 -10" />
            </svg>
        </div>
        <div>
            <h4 class="alert-heading">Se ha eliminado el registro.</h4>
            <div class="alert-description">
                @TempData["Success"]
            </div>
        </div>
    </div>

}

<div class="table-responsive bg-white border rounded overflow-hidden custom-shadow">
    <table class="table  table-vcenter">
        <thead>
            <tr>
                <th>
                    #
                </th>
                <th>
                    Nombre
                </th>
                <th>
                    Código IATA
                </th>
                <th>
                    País
                </th>
                <th>
                    Fecha Fundación
                </th>
                <th>
                   Teléfono
                </th>
                <th>
                    Estado
                </th>
                <th>Opciones</th>
            </tr>
        </thead>
        <tbody id="tablaBody">
     @{
         bool hayDatos = false;
     }

    @foreach (var item in Model) {
            hayDatos = true;
            <tr>
                <td class="fw-bold">
                    @Html.DisplayFor(modelItem => item.id_aerolinea)
                </td>
                <td>
                    <div class="d-flex align-items-center gap-2 fw-bold">
                            <div class="text-center bg-blue-lt p-3 rounded-circle text-primary">
                                <i class="fa-regular fa-building fs-2"></i>
                            </div>
                           
                            @Html.DisplayFor(modelItem => item.nombre)
                    </div>
                    
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.codigo_iata)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.pais)
                </td>
                <td>
                        @item.fecha_fundacion.ToString("dd/MM/yyyy")
                </td>
                <td>
                        <i class="fa-solid fa-phone text-muted"></i>
                    @Html.DisplayFor(modelItem => item.telefono)
                </td>
                <td>
                   <span class="badge bg-green-lt text-green-lt-fg text-uppercase rounded">
                        @Html.DisplayFor(modelItem => item.estado)
                   </span>
                </td>
                <td>
                   <a asp-action="Edit" asp-route-id="@item.id_aerolinea">
                            <i class="fa-solid fa-pen me-2 text-primary btn-animation fs-2"></i>
                        </a>
                    @* @Html.ActionLink("Edit", "Edit", new { id=item.id_aerolinea}) *@
                    @*  @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) | *@
                    @*  @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) *@
                    <form id="frm_del_aerolinea" asp-controller="AerolineasActivas" asp-action="Delete" method="post" class="d-inline-block">
                            <input type="hidden" name="id" value="@item.id_aerolinea" />
                            <button type="submit" class="bg-transparent m-0 p-0 border-0 btn-animation"><i class="fa-solid fa-trash text-danger fs-2"></i></button>
                    </form>
                </td>
            </tr>
    }
    @if (!hayDatos)
    {
        <tr>
            <td colspan="8" class="text-center py-5">
                <div class="d-flex flex-column align-items-center justify-content-center">
                    
                    <i class="fa-regular fa-face-frown text-muted" style="font-size: 50px;"></i>
                
                    <span class="mt-3 fs-4 text-secondary">
                        No hay información para mostrar
                    </span>

                </div>
            </td>
        </tr>
    }

        </tbody>
    </table>
</div>